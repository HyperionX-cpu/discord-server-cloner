# Simple Discord Logger Script

A small testing playground for discord.js event listeners and basic discord api sandbox utilities.

## Requirements
- Node.js 18+
- npm

## Usage
1. Run `npm install`
2. Run `npm start`

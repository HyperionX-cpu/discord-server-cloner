{
  "name": "discord-cloner-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Discord Server & Channel Cloner with Web Dashboard and Discord.js Bot",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm --prefix client run build",
    "build": "npm run install:all && npm run build:client",
    "start": "node server/src/index.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "keywords": ["discord", "bot", "cloner", "dashboard", "vite", "react", "express"],
  "author": "",
  "license": "MIT"
}
